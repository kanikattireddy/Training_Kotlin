package com.example.javalib2



fun main()
{
    /* println("hello world")
     var myName : String = "pavan"
     var college ="JNTUA"
     college=" MITS"


     val id =100 // val is constant(final)
    // id = 101

     println(" my name is $myName and length is ${myName.length} and id is $id and my college is $college")

     var age : Int = 26
     var doubleValue : Double = age.toDouble(); // explicitly need to convert the int to double

     */
    /* var day = 9
     when(day)
     {
         1-> println(" today is monday")
         2-> {
             println(" today is tuesday")
             println("welcome")
         }
         3,4,5-> println(" days of the week")
         in 8..15 -> println(" not days of the week") // to ckeck within the range of the values(range operator)
         else -> println(" no days entered")
     }


     for(i in 1..10) {
         println(i) // it will print 1 to 10
     }


     // for (i in 10 ..1)// it will not print 10 to 1
     for(i in 10 downTo 1) // for printing 10 to 1
     {
         println(i)
     }

     for(i in 10 downTo 1 step 2) // for printing 10 8 6 4 2
     {
         println(i)
     }

     var myarr = arrayOf(11,34,56,89,102)
     for(i in myarr)
     {
         println(i); // here i is printing value
     }
   for(i in 0..myarr.size-1)
   {
       println(" index is $i and value is ${myarr[i]}")  // here i is index value of array
   }


     var myarr = arrayOf(11,34,56,89,102)
     myarr.forEach {
         println(it) // it is the object of foreach to  printing the stream data of array
     }

     var mylist = mutableListOf<Int>(55,89,34,97) // here <Int> means we are specifying only integer list
     mylist.add(12)

     mylist.forEach {
         println(it)
     }

     */
    var i=2;
    while(i<=100)
    {
        println(i)
        i=i+2
    }
}